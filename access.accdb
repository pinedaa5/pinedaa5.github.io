On your computer create a folder `music-site`, place:
   - `index.html` (above)
   - `styles.css` (above)
   - `Artists.csv`, `Albums.csv`, `Songs.csv`, and `MusicCollection.accdb` (your files)
   - `README.md` (above)
2. In that folder open terminal and run:
```bash
git init
git add .
git commit -m "Add download page + data files"
git branch -M main
git remote add origin https://github.com/<YOUR_USERNAME>/<YOUR_REPO>.git
git push -u origin main
